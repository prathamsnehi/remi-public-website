# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: yarn install --frozen-lockfile && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_REMI_PUBLIC }}
          channelId: live
          projectId: remi-public

      - name: Install Cloud Functions Dependencies
        run: cd functions && yarn install --frozen-lockfile

      - name: Deploy Functions and Storage Rules
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only functions,storage
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_REMI_PUBLIC }}
          PROJECT_ID: remi-public
